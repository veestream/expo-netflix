<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Main App Theme -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:enforceNavigationBarContrast" tools:targetApi="29">true</item>
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:statusBarColor">#000000</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.SplashScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Correct references to resources -->
        <item name="android:windowSplashScreenBackground">@color/splashscreen_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splashscreen_logo</item>
        <item name="android:postSplashScreenTheme">@style/AppTheme</item>
        <!-- Enum value must be valid -->
        <item name="android:windowSplashScreenBehavior">icon_background</item>
    </style>

</resources>
